name: saaserp_shared
description: Gemeinsame Modelle und DTOs für saasERP-Backend, User-App und Kunden-App.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  dart_jsonwebtoken: ^2.14.0
  encrypt: ^5.0.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
