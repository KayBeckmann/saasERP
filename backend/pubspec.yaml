name: backend
description: saasERP Backend (Dart Frog) — Multi-Tenant-API für User-App und Kunden-App
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  bcrypt: ^1.1.3
  dart_frog: ^1.1.0
  dart_jsonwebtoken: ^2.14.0
  postgres: ^3.4.0
  saaserp_shared:
    path: ../shared

dev_dependencies:
  dart_frog_lint: ^0.1.0
  mocktail: ^1.0.0
  test: ^1.0.0
